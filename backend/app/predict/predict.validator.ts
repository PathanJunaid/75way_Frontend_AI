
import { body } from 'express-validator';

export const predict = [
    // body('months')
    //     .notEmpty().withMessage('Months is required')
    //     .isInt({ min: 1 }).withMessage('Months must be a positive integer'),
];